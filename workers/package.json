{
  "name": "review-edge-workers",
  "version": "1.0.0",
  "description": "Cloudflare edge workers for token check, EXIF scrubbing, and dynamic WebP/AVIF transformation.",
  "main": "edge-worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.8",
    "exifr": "^7.1.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.1"
  }
}
